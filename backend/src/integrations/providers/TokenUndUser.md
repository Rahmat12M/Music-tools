# Wenn Rahmatulla mit Middleware und token Storage fertig ist
- Die provider korrigieren
    - currentuser -> userId
    - Token storage integrieren. 
- router bzw endpoints verändern
    - müssen zuerst die middleware ausführen und token prüfen. 